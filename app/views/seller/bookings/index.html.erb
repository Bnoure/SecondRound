<div class="d-flex flex-column">
  <%@bookings.each do |booking|%>
    <span class="tdn">
      <div class="card-store">
        <div class="card-store-infos">
          <h2><%=booking.game.title%></h2>
          <div class="d-flex justify-content-center">
          <div class="d-flex flex-column justify-content-center">
          <p><%= booking.user.first_name %> <%= booking.user.last_name %></p>
          <p><%= booking.created_at.strftime('%d/%m/%Y') %></p>
          <p><%= booking.limit_date.strftime('%d/%m/%Y') %></p>
        </div>
        <div class="d-flex flex-column justify-content-center">
          <p><i class="fa-regular fa-circle-xmark fa-sm" style="color: #000000;"></i></p>
          <p><i class="fa-solid fa-check fa-sm" style="color: #000000;"></i></p>
        </div>
        </div>
      </div>
      </div>>
      </span>
  <%end %>
</div>
